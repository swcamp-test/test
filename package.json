{
  "name": "test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8080 --host",
    "start": "node server.js"  // 추가된 부분: Elastic Beanstalk에서 애플리케이션을 시작하는 스크립트
  },
  "dependencies": {
    "vue": "^3.4.21",
    "express": "^4.17.1"  // 추가된 부분: Express 의존성
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8"
  }
}
